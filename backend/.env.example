# PhoneGPT Webhook Server Configuration

# Server Configuration
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=info

# Optional: Database for persistent event storage
# DATABASE_URL=postgresql://user:password@localhost/phonegpt

# Optional: Redis for better event queue
# REDIS_URL=redis://localhost:6379

# Optional: Push Notifications (for real-time updates to iOS app)
# APNS_KEY_ID=your_key_id
# APNS_TEAM_ID=your_team_id
# APNS_BUNDLE_ID=com.codeofhonor.PhoneGPT

# Optional: Authentication token for webhook endpoints
# WEBHOOK_SECRET=your_secret_token_here
