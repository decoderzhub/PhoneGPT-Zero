# PhoneGPT MentraOS Server Configuration

# Required: Your MentraOS API key from https://console.mentra.glass
MENTRAOS_API_KEY=your_api_key_here

# Required: Package name registered in MentraOS Console
# Must match exactly what you registered
PACKAGE_NAME=com.codeofhonor.phonegpt

# MentraOS SDK port (default: 3000)
PORT=3000

# API server port for PhoneGPT iOS app to poll (default: 3001)
API_PORT=3001

# Optional: Log level (debug, info, warn, error)
LOG_LEVEL=info
