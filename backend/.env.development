# ============================================================================
# PhoneGPT MentraOS Server Configuration
# ============================================================================

# ============================================================================
# MentraOS Integration (REQUIRED)
# ============================================================================
# Get your API key from: https://console.mentra.glass
# Keep this secure - do NOT commit to version control
MENTRAOS_API_KEY=your-mentraos-api-key-here

# Package name registered in MentraOS Console
# Format: com.yourcompany.appname
PACKAGE_NAME=org.codeofhonor.phonegpt

# Frontend URL (MentraOS glasses will connect to this)
FRONTEND_URL=https://phonegpt.systemd.diskstation.me

# ============================================================================
# Server Configuration
# ============================================================================
PORT=8112
NODE_ENV=development

# ============================================================================
# Database Configuration
# ============================================================================
DB_PATH=./phonegpt.db

# For PostgreSQL production:
# DATABASE_URL=postgresql://user:password@localhost:5432/phonegpt_db
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=phonegpt
# DB_USER=phonegpt_user
# DB_PASSWORD=your_password_here

# ============================================================================
# JWT Authentication Configuration
# ============================================================================
# Generate a strong secret key for production:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345
JWT_EXPIRY=30d

# ============================================================================
# Frontend Configuration
# ============================================================================
REACT_APP_API_URL=http://localhost:8112
REACT_APP_WS_URL=ws://localhost:8112

# ============================================================================
# CORS Configuration
# ============================================================================
# Allowed origins for API requests
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,https://phonegpt.systemd.diskstation.me

# ============================================================================
# Logging & Monitoring
# ============================================================================
LOG_LEVEL=info

# ============================================================================
# Security Configuration
# ============================================================================
# Bcrypt hashing rounds (higher = more secure but slower)
# 10 rounds takes ~500ms, acceptable for most use cases
# 12 rounds takes ~1s, recommended for production
BCRYPT_ROUNDS=10

# Session timeout in milliseconds (30 minutes)
SESSION_TIMEOUT_MS=1800000

# ============================================================================
# Feature Flags
# ============================================================================
ENABLE_EXPORT=true
ENABLE_SESSION_HISTORY=true
MAX_SESSIONS_PER_USER=50
MAX_MESSAGES_PER_SESSION=1000

# ============================================================================
# AI Integration
# ============================================================================
# Anthropic API for Claude integration
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# Optional: OpenAI integration
# OPENAI_API_KEY=sk-proj-your-api-key-here

# Local AI Model (MLX Swift integration for iPhone)
# Path to local LLM model files
# LOCAL_AI_MODEL_PATH=./models/llama-7b

# ============================================================================
# Third-party Services (Optional)
# ============================================================================
# Error tracking
# SENTRY_DSN=https://your-key@sentry.io/your-project-id

# Analytics
# MIXPANEL_TOKEN=your-token-here
# DATADOG_API_KEY=your-api-key-here

# ============================================================================
# Email Configuration (Optional)
# ============================================================================
# For password reset emails
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ============================================================================
# Production Deployment Checklist
# ============================================================================
# Before deploying to production:
#
# 1. Security
#    - Change JWT_SECRET to a strong random value
#    - Keep API keys secure (use secrets manager, not .env in git)
#    - Set NODE_ENV=production
#    - Use HTTPS (redirect HTTP to HTTPS)
#
# 2. Database
#    - Switch to PostgreSQL for production
#    - Set up automated backups
#    - Enable encryption at rest
#
# 3. CORS
#    - Update CORS_ORIGIN to your production domain only
#    - Remove localhost URLs
#
# 4. Monitoring
#    - Configure error tracking (Sentry)
#    - Set up performance monitoring
#    - Enable request logging
#
# 5. Scaling
#    - Use load balancer
#    - Set up rate limiting
#    - Configure caching
#
# ============================================================================
